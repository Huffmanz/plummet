[gd_resource type="Resource" script_class="ModifierData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/modifier_data.gd" id="1_surge"]

[resource]
script = ExtResource("1_surge")
id = "Surge"
display_name = "Surge"
initial = "Z"
badge_color = Color(0.92, 0.85, 0.12, 1.0)
trigger = "clear"
description = "On clear, the next piece you drop has its base clear value ×3 if it clears on the same turn."
